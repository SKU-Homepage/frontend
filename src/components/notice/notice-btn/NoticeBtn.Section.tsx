import { ReactNode } from "react";

const NoticeBtnSection = ({ children }: { children: ReactNode }) => {
  return <section className="w-full h-full">{children}</section>;
};

export default NoticeBtnSection;
